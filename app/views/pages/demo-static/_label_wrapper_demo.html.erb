<!-- 親要素から id を受け取りそのまま親要素に返すために、ローカル変数として id を定義します。 -->
<% id = id %>

<fieldset class="flex flex-col gap-1">
  <label for="<%= id %>" class="label"><%= label %></label>

  <%= yield id %>
</fieldset>
