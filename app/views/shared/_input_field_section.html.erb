<% errors = local_assigns[:errors] || [] %>

<div class="flex flex-col gap-1">
  <%= yield(errors.present? ? "border-error focus:outline-error" : "") %>

  <% if errors.any? %>
    <% errors.each do |error| %>
      <p class="text-sm text-error"><%= error %></p>
    <% end %>
  <% end %>
</div>
