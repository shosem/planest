<% errors = Array(local_assigns[:errors]).compact %>
<% field_class = errors.any? ? "border-error focus:outline-error" : "" %>

<div class="flex flex-col gap-1">
  <%= yield(field_class) %>
  <%= render "shared/input_error_messages", messages: errors %>
</div>
